{
  "name": "openspritekit-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser end-to-end tests for the OpenSpriteKit smoke-test WASM app. Validates SKScene → SKActionRunner → CAWebGPURenderer boot + render in headless Chromium, including the dynamic addChild + nested compound action regression.",
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "serve": "node server.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "swift-wasm-testing": "github:1amageek/swift-wasm-testing"
  }
}
